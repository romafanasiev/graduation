import globalStyles from "../../utils/styles/style-vars";

const messageTableStyles = {
  sorting: {
    display: "flex",
    alignItems: "center",

    color: globalStyles.vars.fontColor,
    backgroundColor: globalStyles.vars.whiteColor,
  },
};

export default messageTableStyles;
